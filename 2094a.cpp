#include <bits/stdc++.h>
using namespace std;
using str = string;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--) {
        str a, b, c;
        cin >> a >> b >> c;
        cout << a[0] << b[0] << c[0];
        cout << '\n';
    }
}