#include <bits/stdc++.h>

using namespace std;
using ll = long long;
void solve() {
    ll n, m, i, j;
    cin >> n >> m >> i >> j;
    cout << 1 << ' ' << 1  << ' ' << n << ' ' << m << '\n';
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int t;
    cin >> t;
    while (t--) solve();

}