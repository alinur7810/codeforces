#include <bits/stdc++.h>;
using namespace std;
 
int main(){
    int x = 10;

    if (x > 20) {
    // Выполнится, если x больше 20
    } 
    else if (x == 10) {
    // Выполнится, если первое условие ложно, но x равен 10
    } 
    else {
    // Выполнится во всех остальных случаях
    }

}